{
  "name": "request_test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node src/server.js",
    "dev": "ts-node-dev -r tsconfig-paths/register --inspect=4321 --respawn --transpile-only --ignore-watch node_modules --no-notify src/server.ts",
    "build": "babel server --extensions \".js,.ts\" --out-dir dist --copy-files --no-copy-ignored"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-simple-stats": "^1.1.3",
    "helmet": "^4.6.0"
  },
  "devDependencies": {
    "ts-node": "^10.3.0",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.4.4"
  }
}
